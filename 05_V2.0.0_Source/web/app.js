const BUILD='KZ-ENTERPRISE-V2-BETA-20260916-R1';const $=id=>document.getElementById(id);async function api(path,options){const r=await fetch(path,options);if(!r.ok)throw new Error(`本地服务返回 ${r.status}`);return r.json()}function esc(value){return String(value).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function list(id,items,empty){$(id).innerHTML=(items?.length?items:[empty]).map(x=>`<li>${esc(x)}</li>`).join('')}function tasks(id,data){const items=data?.tasks||[];$(id).className=items.length?'':'empty';$(id).innerHTML=items.length?items.map(t=>`<div class="task"><strong>${esc(t.title)}</strong><span class="tag">${esc(t.risk==='low'?'低风险建议':t.risk)}</span><p>${esc(t.reason)} · ${esc(t.action)}</p></div>`).join(''):'尚未生成明日计划'}function showReview(r){if(r.status!=='generated')return;$('review-headline').textContent=r.summary.headline;$('review-policy').textContent=r.summary.data_policy;list('problems',r.problems,'今日未发现已验证问题');list('opportunities',r.opportunities,'今日没有足够数据识别机会');tasks('review-plan',r.tomorrow_plan)}async function loadSummary(){const s=await api('/api/operation-summary/today');$('summary-headline').textContent=s.headline;$('summary-policy').textContent=s.data_policy;const entries=Object.entries(s.verified_metrics||{});$('metrics').innerHTML=entries.length?entries.map(([k,v])=>`<div class="metric"><b>${esc(v)}</b><small>${esc(k)}</small></div>`).join(''):'<div class="empty">没有已验证指标</div>';$('missing').textContent=s.missing_metrics?.length?`尚未接入：${s.missing_metrics.join('、')}`:'今日指标已完整接入'}async function loadPlan(){const p=await api('/api/tomorrow-plan/latest');$('plan-date').textContent=p.date?`${p.date} 明日计划`:'尚未生成';tasks('plan-list',p)}async function loadHistory(){const h=await api('/api/history');$('history-list').innerHTML=h.items?.length?h.items.map(r=>`<article class="history-card"><div class="meta">${esc(r.generated_at)}</div><h3>${esc(r.summary.headline)}</h3><p>${esc(r.problems[0]||'没有已验证问题')}</p></article>`).join(''):'<article class="empty">尚无历史记录；生成今日复盘后会自动保存。</article>'}async function loadMemory(){const [m,e]=await Promise.all([api('/api/memory'),api('/api/experiments')]);$('memory-list').innerHTML=m.entries?.length?m.entries.map(x=>`<div class="task"><strong>${esc(x.statement)}</strong><span class="tag">${esc(x.category)}</span><p>${esc(x.evidence||'未附证据')}</p></div>`).join(''):esc(m.message||'尚无学习记录');$('experiment-list').innerHTML=e.experiments?.length?e.experiments.map(x=>`<div class="task"><strong>${esc(x.name)}</strong><p>${esc(x.result||'进行中')}</p></div>`).join(''):'尚无实验记录'}function toast(message){$('toast').textContent=message;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),2400)}document.querySelectorAll('.nav').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.nav,.page').forEach(x=>x.classList.remove('active'));b.classList.add('active');$(b.dataset.page).classList.add('active')}));$('generate').addEventListener('click',async()=>{const b=$('generate');b.disabled=true;b.textContent='正在生成';try{const r=await api('/api/daily-review/generate',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});showReview(r);await Promise.all([loadSummary(),loadPlan(),loadHistory(),loadMemory()]);toast('今日复盘已生成并保存')}catch(e){toast(e.message)}finally{b.disabled=false;b.textContent='重新生成今日复盘'}});(async()=>{try{const s=await api('/api/status');if(s.build!==BUILD)throw new Error('前后端构建版本不一致');document.querySelector('.service').classList.add('ok');$('service-text').textContent='本地服务运行正常';showReview(await api('/api/daily-review/latest'));await Promise.all([loadSummary(),loadPlan(),loadHistory(),loadMemory()])}catch(e){$('service-text').textContent=e.message;toast(e.message)}})();console.log(`KAZUIZHI_BUILD=${BUILD}`);
