# 卡嘴智 R8 Final 现场配置说明

## 2026-09-20 现场复检结论

- 已修复“手机画面只闪现零点几秒后消失”：刷新期间保留上一帧，不再先清空画面；截图链路改为轻量 ADB 校验与顺序刷新。
- 真机 `M5QNU22319503960` 实测返回有效 PNG：约 2.08 MB；首帧约 1.6 秒，重复请求缓存命中约 0 毫秒，下一新帧约 1.2 秒。实际速度会受 USB 和手机负载影响。
- 已增加首页总控金字塔、R8 中文状态、按钮持久反馈、内容草稿提交 R8 审核和 0→10 闭环体检。
- 当前真实经营数据只读源与双向运营桥已连接；外部大模型 API 尚未配置。
- 当前没有任何平台账号完成绑定授权，因此真实平台采集、自动发布、评论/私信回收和发布 URL 仍被阻塞。
- RTX 3060 已检测到，但视频模型目录和成片输出目录尚未配置，因此不能声称已自动生成视频。
- 搜索站点地图、Search Console 或站长平台尚未接入，因此不能声称内容已被搜索引擎收录。

以上阻塞会在“R8 增长总控 → 审计与体检 → 运行 0→10 闭环体检”中显示具体处理动作；软件不会用演示数据冒充上线结果。

## 必须由现场完成的授权

以下不是软件伪装成已完成的项目，而是必须由真实现场条件完成的上线验收项：

- 抖音、视频号、小红书、快手、微博等平台账号需要在真实 App 内人工登录并处理验证码/人脸/风控。
- 官方平台 Connector 需要相应平台授权；没有授权时可人工导入真实信号和回执，但不会显示官方接口已连接。
- RTX 3060 视频工厂需要本机 NVIDIA 驱动、兼容模型和输出目录；软件不会下载来历不明的模型。
- 订单、收入和履约归因需要已验证只读经营数据源或老板人工确认。
- 代码签名证书未随仓库提供。Final 已调整高误报打包特征并执行 Defender 扫描；Windows 仍可能显示“未知发布者”，这与病毒检出是两类提示。

发生平台页面变化、账号风控或真实业务字段差异时，应保留失败回执并在审计中心查看，不要绕过平台安全机制。
