# V2.0.0 Beta 版本迁移交接 / 测试指南

日期：2026-09-15。状态：迁移测试版，Actions 自动验证已通过，用户实机验收待完成；不标记为稳定版。

## 本次问题与修复

旧 V2 workflow 仅更换 Artifact 名称，仍引用 `03_V1.9.5_Source` 和旧 spec，且上传 dist 而非安装器。
现建立独立 `05_V2.0.0_Source`，前端固定为该目录的 `web/`，含 backend、config、core/version、version/manifest.json 和 docs。
EXE、前端、HTTP 状态、Windows 文件属性、安装器、快捷方式和输出文件均采用 V2.0.0 Beta 标识。
构建入口 `04_Build/build_v2_windows.ps1`，spec `04_Build/kazuizhi_v2.0.0.spec`，构建输出 `build_v2` / `dist_v2` / `installer_output_v2`。
安装器明确使用 `Kazuizhi_AI_V2.0.0_Beta_Setup.iss`，不通过通配符选取旧脚本。

## R3 和数据保护

原 `03_V1.9.5_Source`、原有 `04_Build` 文件、R3 workflow 和所有既有 docs 原样保留。
`04_Build/v2/r3_preservation.json` 对这些文件逐项保存 SHA256，CI 强制验证。
迁移前 main：`ec921b14714a963ee7b012c3d7cc2265cfa66705`。
既有 `baseline/v1.9.5-enterprise-r3-20260915` 分支不修改；本次读取分支头为 `7c7ca4067490cb9ff386370e5bfa51b84746fcd8`。
历史交接记录的成功 R3 构建为 `34965930117`，本文没有重新认证该历史安装包。

V2 使用独立 AppId `{C8148D45-198A-43BF-BC84-922000000001}` 和当前用户安装目录：
`%LOCALAPPDATA%/Programs/Kazuizhi_AI_Enterprise_V2.0.0_Beta`。
错误日志保存在 `%LOCALAPPDATA%/Kazuizhi_AI_Enterprise_V2.0.0_Beta/logs`，不写入程序资源或 R3 数据目录。
本次不进行历史业务数据迁移，不删除旧数据。回退时退出 V2，启动原 R3；无需覆盖 R3 文件。
仍使用 8876 端口。测试前先关闭 R3；端口被占用会退出并记录错误，不会打开错误版本页面。

## 回归与已知功能边界

- 保留 Enterprise 驾驶舱、增长分析、SEO/GEO、AI 任务、调度与健康区块。
- 源码运行与打包运行使用同一前端；移除旧启动器额外打开的 8765 旧页面。
- 旧只读 API 路径统一至 8876；未接入统计返回 null 和 not_connected，不能冒充真实数据。
- 当前仓库的业务卡片和任务功能本来就有占位；本次不宣称恢复 V1.8.x 全部能力。
- AI 每日复盘、总结、问题分析、增长机会、明日计划、历史记录、云同步及长期记忆仍待后续功能恢复。
- 安装器未做代码签名，Windows 实机视觉和信任提示验收待用户测试。

## 自动测试和交付

CI 验证旧源码/文档哈希不变、V2 资源与版本一致、实际 HTTP 启动及查询、端口冲突退出、打包资源逐字节一致。
Windows runner 执行静默安装、安装后运行、Windows 版本信息检查、覆盖重装、用户自建文件保留及卸载。
只有全部通过才上传 Setup 和 Portable。安装包旁保存 `BUILD_RECORD.json`（commit、run、checks、SHA256）与 `SHA256SUMS.txt`。
Artifact：`Kazuizhi_AI_Enterprise_V2.0.0_Beta_Setup`。
真正安装程序：`Kazuizhi_AI_Enterprise_V2.0.0_Beta_Setup.exe`。
Portable 解压顶层目录：`Kazuizhi_AI_Enterprise_V2.0.0_Beta`。

## 用户测试步骤

1. 关闭正在运行的 R3，下载并解压 Setup Artifact，运行其中的 Setup.exe。
2. 使用新建的 V2.0.0 Beta 快捷方式，确认只打开一个 Dashboard。
3. 检查窗口、页面和版本信息均为 V2.0.0 Beta，页面 build 为 `KZ-ENTERPRISE-V2-BETA-20260915`。
4. 确认原 R3 安装目录及历史资料仍在；先退出 V2 后可重新打开 R3。
5. 反馈安装、启动及界面结果。功能恢复按既有 V2 计划另行验收。

## 构建交付记录

主分支 Run `34989279784` 成功，构建源码 `d1e797eda9bfa68634ab3bf961eba851074fecc7`。详见 [V2.0.0_BETA_TEST_REPORT.md](V2.0.0_BETA_TEST_REPORT.md)，包含下载链接、文件校验值与测试证据。
